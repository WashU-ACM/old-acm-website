<h1>ACM Showcase</h1>
<p class="lead">
  Welcome to the ACM Showcase.  This is where members of the ACM community can show off their work for current and future ACM members.
</p>

<% if current_user %>
<div class="text-center">
  <%= link_to "Submit Entry", new_acm_project_path, :class => "btn btn-primary" %>
</div>
<% else %>
<p class="lead">
  Simply sign in, submit a description of your work, and wait for it to be processed.
</p>
<% end %>



<% if @acm_projects != nil %>
<% @acm_projects.each do |acm_project| %>
<div class="project-holder">
  <div class="acm-project-header">
    <br/>
    <%= image_tag showcase_image_url(acm_project), class: "img-polaroid acm-project-image-header" %>
    <h3><%= acm_project.name %></h3>
    <p class="acm-project-submitted-by">
      Submitted by <%= acm_project.owner.full_name %>
    </p>
    <p class="acm-project-created-at">
      <%= acm_project.created_at.to_formatted_s(:month_day_year_words) %>
    </p>
  </div>
  <div class="project-description">
   <%= truncate(strip_tags(acm_project.description), length: 200) %>
   <%= link_to "Read more", acm_project %>
 </div>
</div>
<% end %>
<% end %>

