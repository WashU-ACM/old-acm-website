<%= form_for(@icpc_entry) do |f| %>
  <% if @icpc_entry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@icpc_entry.errors.count, "error") %> prohibited this icpc_entry from being saved:</h2>

      <ul>
      <% @icpc_entry.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
